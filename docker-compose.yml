version: '3.8'

services:
  mcp-app:
    build: .
    container_name: mcp-multi-agent-app
    ports:
      - "8501:8501"
    environment:
      - PORT=8501
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    restart: always
